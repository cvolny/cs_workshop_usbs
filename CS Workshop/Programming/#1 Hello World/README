First, check out the Flow Diagram that explains what this program is supposed to in what order. All of these programs today are very simple linear programs with no decision making or error checking.

Each program is written in C++ and Python, two very different programming languages. Each example can be read in a simple text editor (like gedit) and includes a <source-file>.readme file that explains how to run it.
